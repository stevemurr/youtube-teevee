FROM node:20-alpine

# Install build dependencies for native modules (sqlite3)
RUN apk add --no-cache python3 make g++

WORKDIR /app

# Expose API port
EXPOSE 3001

# Install deps at runtime, then start dev server
CMD sh -c "npm install && npm run dev"
