{
  "name": "youtube-teevee",
  "version": "1.0.0",
  "description": "YouTube TV Guide Web App - A modern web application that transforms YouTube subscriptions into a traditional TV experience",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:api": "cd api && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:api && npm run build:frontend",
    "build:api": "cd api && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "install:all": "npm install && cd api && npm install && cd ../frontend && npm install",
    "clean": "rm -rf node_modules api/node_modules frontend/node_modules",
    "test": "echo \"No tests configured yet\"",
    "lint": "cd api && npm run lint && cd ../frontend && npm run lint"
  },
  "dependencies": {
    "express": "^4.21.2",
    "dotenv": "^16.4.7",
    "http-proxy-middleware": "^3.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "MIT"
}